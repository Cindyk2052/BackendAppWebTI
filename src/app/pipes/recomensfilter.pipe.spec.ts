import { RecomensfilterPipe } from './recomensfilter.pipe';

describe('RecomensfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecomensfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
