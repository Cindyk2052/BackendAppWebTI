import { VideosfilterPipe } from './videosfilter.pipe';

describe('VideosfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VideosfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
